import {CheckAuthorityPipe} from './check-authority.pipe';

describe('CheckAuthorityPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckAuthorityPipe();
    expect(pipe).toBeTruthy();
  });
});
